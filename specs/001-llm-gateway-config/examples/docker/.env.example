# Environment Variables for Docker Compose
# Purpose: Template for local development environment variables
# Usage: Copy to .env and fill in your values

# LiteLLM Gateway Configuration
LITELLM_MASTER_KEY=sk-local-gateway
LITELLM_LOG=INFO

# Google Cloud Configuration
# Option 1: Use service account file (recommended for Docker)
GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-key.json

# Option 2: Use gcloud application-default credentials (for local development)
# Leave GOOGLE_APPLICATION_CREDENTIALS unset and run:
# gcloud auth application-default login
# Then mount ~/.config/gcloud to the container

# Optional: Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# Optional: Proxy Configuration (if behind corporate proxy)
# HTTPS_PROXY=http://proxy.example.com:8080
# HTTP_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1,.internal

# Optional: SSL Certificate (if using corporate proxy with SSL inspection)
# SSL_CERT_FILE=/app/corporate-ca-bundle.crt
# REQUESTS_CA_BUNDLE=/app/corporate-ca-bundle.crt
